Setup
=====
ssh-keygen -t rsa -b 4096 -C "rantaoca@gmail.com"
cat ~/.ssh/id_rsa.pub
https://github.com/settings/ssh/new

cd ~
git clone git@github.com:rantaoca/nix-profile.git
mv nix-profile .ranconf
cd .ranconf
# optional, installs tmux, vim, and git
sudo bash install.sh
bash setup.sh

